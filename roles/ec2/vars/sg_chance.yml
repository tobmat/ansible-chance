aws_security_groups:
  - name: ec2-chance
    description: ec2-chance - generated by ansible
    vpc_id: "{{ vpc_id }}"
    region: "{{ region }}"
    rules: 
      - proto: tcp
        ports:
          - 80
          - 22
        cidr_ip: 0.0.0.0/0
